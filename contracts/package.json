{
  "name": "sats-dca-contract",
  "version": "1.0.0",
  "description": "SATS.DCA — On-Chain Dollar Cost Averaging vault on Bitcoin L1 via OP_NET",
  "main": "index.js",
  "scripts": {
    "build": "asc src/index.ts --config asconfig.json --outFile build/DCAVault.wasm --textFile build/DCAVault.wat",
    "build:release": "asc src/index.ts --config asconfig.json --outFile build/DCAVault.wasm --textFile build/DCAVault.wat --optimize"
  },
  "dependencies": {
    "@btc-vision/btc-runtime": "latest",
    "as-bignum": "latest"
  },
  "devDependencies": {
    "assemblyscript": "latest"
  }
}
