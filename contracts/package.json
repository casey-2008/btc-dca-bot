{
  "name": "sats-dca-contract",
  "version": "1.0.0",
  "description": "SATS.DCA — On-Chain Dollar Cost Averaging vault on Bitcoin L1 via OP_NET",
  "main": "index.js",
  "scripts": {
    "build": "asc src/index.ts --target release --config asconfig.json --uncheckedBehavior never"
  },
  "dependencies": {
    "@btc-vision/btc-runtime": "latest",
    "as-bignum": "latest"
  },
  "devDependencies": {
    "assemblyscript": "latest"
  }
}
